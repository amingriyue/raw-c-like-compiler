1. How to build
  a. cd compiler
  b. make
There will be a executable binary named "compiler"

2. How to compile a c file
  ./compiler ../test/test.c
  Above compilation wil generate two files, one is intermediate representation(test.ir), another is asm file(test.s)

3. There are still many todo tasks, like optimization, assambler and linker.
